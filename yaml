version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"  # Mapeia 3001 externa (livre) para 3000 interna
    volumes:
      - .:/app
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app.rule=Host(`jwogsc0ww88s8s4kows4s8g0.186.249.193.124.sslip.io`)"
      - "traefik.http.routers.app.entrypoints=web"
      - "traefik.http.services.app.loadbalancer.server.port=3000"
